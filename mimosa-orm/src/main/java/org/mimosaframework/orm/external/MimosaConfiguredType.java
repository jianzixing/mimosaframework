package org.mimosaframework.orm.external;

public enum MimosaConfiguredType {
    PACKAGES, ADDITION_CLASSES, CONVERT_NAME, CONVERT_CLASS, SHOW_SQL,
    TRANSACTION_BY_MIMOSA, DEFAULT_DATASOURCE, STRATEGIES, CLUSTER_ENABLE,
    CLUSTER_CONFIG, INIT_DATASOURCE
}
